import styled from 'styled-components';

export const NavItem = styled.li`
    display: flex;
    justify-content: space-evenly;
    color: red;
    border: dotted;
    border-color: red;
    padding: 10px;
`